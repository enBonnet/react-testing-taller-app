import React from "react";
import { render } from "@testing-library/react";
import { ButtonPro } from "./ButtonPro";

// TODO Los comentairos es lo que se espera del test
// Limpiar dom

test("renderizas el boton pro", () => {
  // Renderizar el componente
  // Comprobar que se renderiza correctamente
  // Seleccionar la ".caja"
  // Probar el estado inicial de la "caja"
  // Seleccionar el "botón pro"
  // Validar que esta seleccionado el botón
  // Hacer click sobre el botón
  // Validar los cambios que se esperaba en la "caja
});
