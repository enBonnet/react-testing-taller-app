import React from "react";
import { render, fireEvent } from "@testing-library/react";
import { Form } from "./Form";
import "@testing-library/jest-dom/extend-expect";

test("rellenar los campos del formulario y limpiar los campos al enviarlo", () => {
  // Renderizar el Form
  // Seleccionar el input de usuario
  // Cabiar el valor del input usuario
  // Probar el cambio del valor en el input usuario
  // Seleccionar el input correo
  // Cambiar el valor del input correo
  // Probar el cambio del valor en el input correo
  // Seleccionar el input password
  // Cambiar el calor del input password
  // Probar el cambio del valor en el input password
  // Hacer click en el botón "enviar"
  // Probar que se limien los campos en el formulario
});
